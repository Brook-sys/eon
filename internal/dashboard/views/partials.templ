package views

templ PartialOverview() {
	<div id="overview-partial" class="space-y-4 font-mono text-sm">
		<div class="p-4 bg-[var(--panel)] border border-[var(--border)] rounded-lg">
			<span class="text-[var(--ok)] font-semibold">✓ Fragmento HTMX Overview Ativo</span>
		</div>
	</div>
}
